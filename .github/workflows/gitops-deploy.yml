name: Homelab GitOps Deployment

on:
  push:
    branches: [ main ]
    paths:
      - 'apps/argocd-apps/**'
      - 'apps/k8s-apps/**'
      - '.github/workflows/gitops-deploy.yml'
  workflow_dispatch:

jobs:
  gitops:
    name: Deploy ArgoCD + Sync All Apps
    runs-on: self-hosted   # your self-hosted runner

    env:
      ARGOCD_NAMESPACE: argocd
      ARGOCD_CHART_VERSION: 7.7.10
      GIT_REPO_URL: git@github.com:cterence/homelab-gitops.git
      KUBECONFIG: ${{ github.workspace }}/kubeconfig

    steps:
      # 1️⃣ Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Load SSH key for private repo access
      - name: Load SSH Private Key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      # 3️⃣ Configure SSH Known Hosts
      - name: Configure SSH Known Hosts
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KNOWN_HOSTS }}" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      # 4️⃣ Write kubeconfig from secret
      - name: Configure kubeconfig
        run: |
          echo "${{ secrets.K3S_KUBECONFIG }}" > $KUBECONFIG
          chmod 600 $KUBECONFIG
          echo "✅ Wrote kubeconfig to $KUBECONFIG"

      # 5️⃣ Verify K3S cluster connection
      - name: Verify Kubernetes connection
        run: |
          echo "🔍 Checking Kubernetes context..."
          kubectl cluster-info
          kubectl get nodes -o wide

      # 6️⃣ Ensure ArgoCD namespace exists
      - name: Create ArgoCD namespace if missing
        run: |
          kubectl get ns $ARGOCD_NAMESPACE || kubectl create ns $ARGOCD_NAMESPACE

      # 7️⃣ Cleanup old CRDs to fix Helm ownership issues (first-time setup only)
      - name: Cleanup old ArgoCD CRDs
        run: |
          echo "🧹 Cleaning up old ArgoCD CRDs (safe if running fresh install)..."
          kubectl delete crd applications.argoproj.io appprojects.argoproj.io \
            applicationsets.argoproj.io argocdexports.argoproj.io || true

      # 8️⃣ Install or upgrade ArgoCD
      - name: Install or upgrade ArgoCD via Helm
        run: |
          helm repo add argo https://argoproj.github.io/argo-helm || true
          helm repo update
          helm upgrade --install argocd argo/argo-cd \
            --namespace $ARGOCD_NAMESPACE \
            --version $ARGOCD_CHART_VERSION \
            --set server.service.type=LoadBalancer \
            --set configs.params."server.insecure"=true

      # 9️⃣ Wait until ArgoCD server is ready
      - name: Wait for ArgoCD server
        run: |
          echo "⏳ Waiting for ArgoCD server deployment..."
          kubectl -n $ARGOCD_NAMESPACE wait \
            --for=condition=available deployment/argocd-server --timeout=600s

      # 🔟 Apply App-of-Apps manifest
      - name: Apply App-of-Apps manifest
        run: |
          echo "🚀 Applying App-of-Apps bootstrap..."
          kubectl apply -f apps/argocd-apps/app-of-apps.yaml

      # 11️⃣ Verify ArgoCD Application registration
      - name: Verify App-of-Apps registration
        run: |
          echo "🔍 Checking ArgoCD Application..."
          kubectl -n $ARGOCD_NAMESPACE get app app-of-apps || echo "App-of-Apps not yet created."

      # 12️⃣ Force refresh/sync for all ArgoCD Applications
      - name: Force sync all ArgoCD Applications
        run: |
          echo "🔄 Triggering ArgoCD refresh..."
          kubectl -n $ARGOCD_NAMESPACE get applications.argoproj.io -o name | \
          xargs -I{} kubectl -n $ARGOCD_NAMESPACE patch {} --type merge \
            -p '{"metadata":{"annotations":{"argocd.argoproj.io/refresh":"hard"}}}'
          echo "✅ All ArgoCD Applications refreshed and syncing."

      # 13️⃣ Show ArgoCD admin password
      - name: Display ArgoCD Admin Password
        run: |
          echo "🔑 ArgoCD Admin Password:"
          kubectl -n $ARGOCD_NAMESPACE get secret argocd-initial-admin-secret -o jsonpath='{.data.password}' | base64 -d
